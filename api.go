package main

import (
// "fmt"
// "log"
// "net/http"

// "github.com/c3nsored/qrcode-service/service"
)

// func MakeHandlers(
// 	fn func(http.ResponseWriter, *http.Request, string, chan<- service.Action),
// 	endpoint string,
// 	actionCh chan<- service.Action,
// ) http.HandlerFunc {
// 	return func(w http.ResponseWriter, r *http.Request) {
// 		path := r.URL.Path
// 		method := r.Method

// 		log.Println(fmt.Sprintf("Recieved request [%s] for path: [%s]", method, path))
// 		// id := path[len(endpoint):]
// 		fn(w, r, method, actionCh)
// 	}
// }
